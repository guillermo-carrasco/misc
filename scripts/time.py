# Small script to test the performance of two or more commands (just wall clock time)
# NOTE: It does _not_ pretend to be any clever at all, so better specify all absolute
# paths so that python subprocess.check_call can execute all binaries
